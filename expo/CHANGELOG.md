# @pylo/expo

## 0.6.0

### Minor Changes

- First release. Re-exports `@pylo/react` with SecureStore as the token store,
  accepts a React Native file reference as an upload source, and refetches
  stale queries when the app returns to the foreground. Versioned alongside
  `@pylo/core`.
