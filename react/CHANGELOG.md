# @pylo/react

## 0.6.0

### Minor Changes

- First release. The fifteen hooks, a session provider with login, logout and
  automatic refresh, and a typed client, for single-page React apps. Versioned
  alongside `@pylo/core`.
