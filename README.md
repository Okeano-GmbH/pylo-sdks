#1. create a github personal access token
#1. login with the access token

```
pnpm login --scope=@okeano-gmbh --auth-type=legacy --registry=https://npm.pkg.github.com
```

enter username
enter personal access token
